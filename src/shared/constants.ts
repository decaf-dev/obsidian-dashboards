/**
 * Global event id for when a user presses the ctrl or cmd key
 */
export const EVENT_CTRL_DOWN = "dashboards:ctrl-down";

/**
 * Global event id for when a user releases the ctrl or cmd key
 */
export const EVENT_CTRL_UP = "dashboards:ctrl-up";

/**
 * Global event id for when the border toggle action button is clicked
 */
export const EVENT_BORDER_TOGGLE = "dashboards:border-toggle";

/**
 * Global event id for when the option bar action button is clicked
 */
export const EVENT_OPTION_BAR_TOGGLE = "dashboards:option-bar-toggle";

/**
 * The name of the view that will be registered with Obsidian
 */
export const DASHBOARDS_VIEW = "dashboards";
